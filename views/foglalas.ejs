<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <div class="container">
        <div class="jumbotron">
            <h1 class="display-4">Foglalás</h1>
            <hr class="my-4">
            <h3>Kérem add meg az adatokat!</h3>
          </div>
    <form action="/foglalas" method="post">
        <div class="mb-3 mt-3">
            <label for="">Név: </label>
            <input type="text" class="form-control" name="nev" value="<%= data ? data.nev : '' %>" required><br>
        </div>    
        <div class="mb-3 mt-3">
            <label for="">E-mail cím: </label>
            <input type="text" class="form-control" name="email_cim" value="<%= data ? data.email_cim : '' %>" required><br>
        </div>
        <div class="mb-3 mt-3">
            <label for="">Dátum és Időpont</label>
            <input type="date" class="form-control" name="datum" value="<%= data ? data.datum : '' %>" id="" required><br>
        </div>
        <div class="mb-3 mt-3">
            <label for="">Nézők (mozi esetén) vagy vendégek (szállás esetén) </label>
            <input type="number" class="form-control" name="szemelyek" value="<%= data ? data.szemelyek: '' %>" required><br>
        </div>
        <input type="submit" class="btn btn-primary" value="Foglal">
    </form>
    <br>
    <% for (let index = 0; index < errors.length; index++) { %>
        <p class="alert alert-danger" role="alert"><%= errors[index] %></p>
    <%} %>
    </div>
</body>
</html>